[package]
name = "PlaceRoulleteCore"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = {version = "0.12", features = ["json"]}
tokio = {version = "1", features = ["full"]}
serde = {version = "1.0.228", features = ["derive"]}
base64 = "0.22.1"
bitvec = "1.1.1"
